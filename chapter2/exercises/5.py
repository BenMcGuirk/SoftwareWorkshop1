"""
Write a program that accepts an object's mass and velocity and output its momentum.
Momentum = mass * velocity
"""
mass = float(input("Enter its mass: "))
velocity = float(input("Enter its velocity: "))
momentum = mass * velocity
print(momentum)